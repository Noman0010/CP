int countDivisors(int x) {
    int divisors = 0;
    for (int i = 1; i <= sqrt(x); ++i) {
        if (x % i == 0) {
            divisors += (i == x / i) ? 1 : 2;
        }
    }
    return divisors;
}
